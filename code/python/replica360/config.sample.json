{
    "_comment": "the unit is meter",
    "scene_name": "apartment_0",
    "_comment": "the output image is panorama or cubemap (panorama/cubemap)",
    "render_type": "panorama",
    "_comment": "The c++ render opinions.",
    "render_params": {
        "texture_exposure": 1.0,
        "texture_gamma": 1.0,
        "texture_saturation": 1.0
    },
    "render_view": {
        "_comment": "whether render data the camera at center of scene",
        "center_view": true,
        "_comment": "whether render data the camera at the trajectory",
        "traj_view": true
    },
    "image": {
        "width": 3840,
        "height": 1920,
        "fps": 50
    },
    "_comment": "config the pose-processing",
    "post_proc": {
        "convert_format_traj": true,
        "convert_format_center": true,
        "_comment": "msg file for omniphoto quantitative evaluation",
        "generate_msg_file": true
    },
    "camera_traj": {
        "_comment": "circle/grid/line/random",
        "type": "circle",
        "_comment": "the interval of grid two points",
        "grid_interval": 0.1,
        "_comment": "when lock direction is false, the camera view direction do not change",
        "lock_direction": false,
        "_comment": "the number of the circle path position",
        "circle_step_number": 90,
        "radius": 0.5,
        "_comment": "The center of grid or circle path",
        "center_position": {
            "x": 0.153774,
            "y": -5.88666,
            "z": -0.38
        },
        "_comment": "the camera view direction of 1st position",
        "start_orientations": {
            "x": 0.0,
            "y": 0.0,
            "z": 0.0
        },
        "_comment": "the camera position bounding box, the bbox center is center_position",
        "random_position_bbox": [
            [x_offset_min, x_offset_max],
            [y_offset_min, y_offset_max],
            [z_offset_min, z_offset_max]
        ],
        "_comment": "the camera rotation range (degree)",
        "random_rotation_bbox": [
            [x_rot_min, x_rot_max],
            [y_rot_min, y_rot_max],
            [z_rot_min, z_rot_max]
        ]
    }
}